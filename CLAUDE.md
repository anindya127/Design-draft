# GCSS Website — Project Guide for Claude

## What this is

Full production website for **GCSS (Global Charger System Service)** — an EV charging management platform. Real users, real payments, real accounts. Not a demo. Design polish + correctness both matter.

## Stack & layout

- Frontend lives in `website/frontend/` (Next.js, App Router, TypeScript).
- Pages are under `website/frontend/src/app/[locale]/` — locale-based routing for EN/ZH.
- Shared styles in `website/frontend/src/app/styles/`.
- Translation system: EN/ZH, driven by locale routing + translation objects.

## Pages

index, product, pricing, blog, contact, forum, docs, login, register, forgot-password.

## Deployment

After any changes, deploy to production server **47.242.75.250**. See `DEPLOYMENT_SETUP.md` / `QUICK_DEPLOY.md` for the flow.

## Working agreements

- Focus on visual polish and completeness over new features.
- Keep EN/ZH translations in sync when editing copy.
- Prefer editing existing components/styles over creating new files.

## Session log

Claude should append a short bullet to the log below at the end of each working session so the next session can pick up cold.

### Log

- 2026-04-10: Removed "System Advantages" section (Section 6) from B2B page and its SubNav entry. Kept `advantages.payment*` translation keys since Section 5's Payment Flow card still uses them. Pushed to main (auto-deploy).
- 2026-04-11: Replaced 3D rotating globe on homepage Global Coverage section with interactive 2D Natural Earth world map. Kept same data (China origin + 17 coverage countries) and brown/gold palette. Added d3-zoom (drag to pan, scroll to zoom), hover tooltips, staggered arc draw-in animation from China, pulsing markers, and +/−/reset buttons. File: `website/frontend/src/components/sections/home/GlobeVisualization.tsx`. Pushed to main (auto-deploy).
- 2026-04-11 (later): Discovered GitHub Actions deploy was silently skipping rsync for an unknown length of time — `SSH_KEY`/`CI_HOST`/`CI_USER`/`CI_PATH` secrets are all unset, and the old workflow reported "success" anyway. Patched `.github/workflows/deploy.yml` to `exit 1` when secrets are missing so future breakages surface immediately. Until the user adds the GitHub secrets, manual deploys go via `SFTP_PASSWORD='Gcss123.' npm run deploy` from `website/frontend/` (uses `scripts/deploy.js` → SFTP to `47.242.75.250:/var/www/gcss-website`). Also shipped three debugger-flagged fixes in commit c4d543b: (1) densified globe arcs with `d3.geoInterpolate(64)` to fix China → French Polynesia / Brazil drawing the wrong way, (2) moved the hard-coded English dashboard sentence in `docs/page.tsx` into `docs.dashboard.desc` EN/ZH keys, (3) converted two `<img>` tags (homepage + docs) to `next/image` at 1920×1080. Trusted-by operator list also swapped from global giants (ChargePoint, EVBox, BYD…) to smaller regional operators (Powerdot, Spirii, Atlante, Kople, Voltio, Statiq, Vourity, Etrel, ChargeUp, Plugzio). All deployed via SFTP.
- 2026-04-11 (changelog + payment request): Added **Changelog** section at the end of the homepage — glass-card timeline with 4 versioned entries (v2.4.0 → v2.1.0), version/date/tag chips, animated gold dot markers on a vertical spine, and a "View full release notes" CTA linking to `/blog`. Translations via `changelog.*` keys in both locales. Styles in `sections.css` (`.changelog-section` / `.changelog-timeline` / `.changelog-entry`). Also added a **local-payment request form** inside the Payment Partners section (`src/components/sections/home/PaymentRequestForm.tsx`, client component) — inputs for payment method, country/region, and email, with an inline submit-received confirmation. Translation keys under `payment.request.*`. Deployed via SFTP. Standing user instruction: always push all changes to the production server after any edit.
- 2026-04-11 (review cleanup): Frontend-wide cleanup from superpowers:code-reviewer report. **Globe**: mounted guards for fetch + tooltip setState, CDN fetch `.catch` → error-state UI, `svg.on('.zoom', null)` cleanup, removed no-op fill ternary. **DiagramModal**: rewrote as `DiagramModalProvider` React context (no more `window.openDiagramModal` global), added `role="dialog"`, `aria-modal`, `aria-labelledby`, Escape handler, focus-in/restore, localized title via new `models.modalTitle.b2c/b2b` keys. **b2b page**: `<a>` demo buttons → `<button type="button">`; replaced 9 unguarded `t.raw(...) as string[]` casts with a `rawList()` helper that `Array.isArray`-guards. Inlined demo creds as `DEMO_CREDS` const (removed from translation files). **Deleted dead code**: `lib/api.ts`, `lib/constants.ts`, `hooks/useMagneticEffect.ts`, `hooks/useIntersectionObserver.ts`, `lib/lazy-components.ts`, `components/sections/home/BusinessDiagram3D.{tsx,css}`. **Deps**: uninstalled 17 unused packages (next-themes, react-fast-marquee, react-intersection-observer, react-use-measure, canvas-confetti, embla-carousel-react/autoplay, lottie-react, class-variance-authority, tailwind-merge, clsx, tsparticles + 2 subpkgs, gsap, @gsap/react, framer-motion) → 64 transitive removed. **SEO metadata**: added to `product/page.tsx`; converted blog, contact, login, register, forum, faq, forgot-password, docs from `'use client'` pages into server `page.tsx` → `PageClient.tsx` splits, each with page-specific title/description. **Translations**: added `models.modalTitle.*`, `steps.stepLabel`, `b2b.advantages.paymentTitle`, `docs.copy/copied`; translated `b2b.profit.subtitle` in zh.json. Localized "Step 1/2/3" and "Copy/Copied!" hardcoded strings. Also removed duplicate `generateStaticParams` from `[locale]/page.tsx` (layout already has it). **Header nav a11y**: dropdown triggers are now `<button>` with `aria-haspopup`, `aria-expanded`, `aria-controls`; CSS opens on `:focus-within` and `.open` in addition to `:hover`. **Hygiene**: added `.claude/` to root `.gitignore` and untracked `.claude/settings.local.json`. Build green, all 26 static pages generated. **Not yet deployed** — awaiting user go-ahead.
- 2026-04-20: CSS polish pass in `website/frontend/src/app/styles/pages.css` — added `:focus-visible` states for keyboard navigation and extra `prefers-reduced-motion` fallbacks (hero scene + marquee). Updated `website/frontend/scripts/deploy.js` to load env vars from `.env.local`/`.env` and added `SFTP_*` placeholders to `website/frontend/.env.local`. `npm run build` succeeded. Not deployed yet (needs `SFTP_PASSWORD`).
- 2026-04-21: Implemented **admin roles + admin dashboard**. Backend: added `users.role` (default `user`) with SQLite forward-migration; first registered user auto-bootstraps as `admin`; added protected `GET /api/admin/overview` returning DB counts (users/admins/sessions/blog/forum/forms). Frontend: added `/[locale]/admin` page that checks `/auth/me` role then fetches overview; added `admin.*` i18n keys; updated login/register to use locale-aware router and redirect admins to `/admin` (others to `/demo`). `go test ./...` and `npm run build` succeeded. Not deployed yet (needs `SFTP_PASSWORD`).
- 2026-04-21 (production infrastructure): Created full production deploy infrastructure for **v3.gcss.hk** (staging subdomain). Added: nginx config (`deploy/nginx/v3.gcss.hk.conf` — static frontend + `/api` proxy), systemd service (`deploy/systemd/gcss-backend.service` — auto-restart, hardened), backend deploy script (`deploy/deploy-backend.js` — cross-compile + SFTP + SSH), unified deploy (`deploy/deploy-all.js`), server setup script (`deploy/setup-server.sh`), and first-deploy script (`deploy/first-deploy.js`). Added `.env.production` for frontend (API = `/api` relative). Updated backend CORS to include `v3.gcss.hk`. Committed Go backend to git (was untracked). First deploy uploaded all files (binary + 681 frontend files) via SFTP to server, but SSH setup commands failed due to rate-limiting — server needs manual `systemctl` + `nginx -t` commands. 66 files committed, pushed to GitHub.
- 2026-04-21 (UI overhaul): **Login/Register**: dark brand panel with feature list (replaced amber gradient), password toggle, loading spinner, removed captcha, 2-column form rows, autoComplete attributes. **Forum**: loading skeleton animation, illustrated empty state with CTA, 8 deterministic avatar colors, markdown-stripped excerpts, mobile toggle moved to top. **Global CSS**: larger inputs, hover/error states, `.btn-loading` spinner, `.form-input-wrapper`, themed banners with slide-in animation.
- 2026-04-21 (auth + dashboard): Added **AuthProvider** (React context) wrapping the app — checks token on mount, provides `user/logout/refresh` globally. **Header user menu**: logged-in users see avatar + name dropdown with Dashboard, Admin Panel (admin only), and Logout. **User dashboard** (`/dashboard`): protected page with profile card (avatar, name, role badge, company), 4 info cards (email, phone, member since, status), quick links grid (forum, blog, docs, contact). Login/register now redirect to `/dashboard` and call `refresh()` to update header immediately. 56 pages building. Not yet pushed or deployed — network down.
- 2026-04-21 (production auth): Added rate limiting (10 req/min/IP) on login/register/password-reset. Added `PUT /api/auth/profile` (update name/phone/company), `POST /api/auth/password/change` (verify current + set new), `GET /api/user/dashboard` (forum activity + session count). Added email regex validation and username length check on registration. Security headers: X-Frame-Options DENY, Referrer-Policy. Dashboard now has inline profile editing and password change section. Fixed layout.tsx script tag issue for Next.js 16 compat.
- 2026-04-21 (blog article): Rebuilt blog article page with polished reading experience: hero with author avatar gradients, reading time estimate, tag pills; sticky table of contents sidebar (auto-generated from h2/h3); prose typography (styled headings, blockquotes, code blocks, tables, lists with gold markers); related articles 3-card grid; loading skeleton; responsive (TOC hidden on mobile). Translations: minRead, toc, related.
- 2026-04-21 (blog CMS): Full admin blog management system. **Backend**: CRUD endpoints (POST/PUT/DELETE `/api/admin/blog/posts`) admin-only; DB migration adding status (draft/published), meta_title, meta_description, og_image_url, updated_at; public list now filters by status=published; upload limit 25MB with video (.mp4/.webm) support + auth required. **Frontend**: admin blog list page (`/admin/blog`) with status badges and edit/delete; new post editor (`/admin/blog/new`) and edit page (`/admin/blog/edit/[slug]`); BlogEditor component with split-pane markdown/preview, drag-drop media upload, locale tabs (EN/ZH), SEO sidebar (meta title/desc with char counters, OG image, cover, author, tags), save draft / publish buttons. **SEO**: blog article page.tsx generates dynamic metadata (OG, Twitter Card, hreflang, canonical, JSON-LD Article schema). 60 pages building. Network still down — 7 commits queued locally.
- 2026-04-21: Added optional cross-region CAPTCHA on login (Cloudflare Turnstile for international + Tencent Captcha for China) with backend verification gated by `CAPTCHA_PROVIDER` env. Login now requires CAPTCHA only when configured. Added `btn-full` utility class and updated login button to avoid inline-style lint. `go test ./...` and `npm run build` succeeded.
- 2026-04-22: Committed CAPTCHA frontend (CaptchaWidget + login wiring + env docs + B2B/B2C images) as `26d2230` and deployed all 948 files to 47.242.75.250 via SFTP (5m 33s). Build green (26 static pages). Commit not yet pushed to GitHub — branch is 22 commits ahead of origin/main.
- 2026-04-22 (B2B/B2C modal): `4a00a8c` reverts the Business Models card media change and moves the new isometric illustrations (B2B.png, B2C.png) into the existing Learn-More modal instead of onto the cards. `<Image fill objectFit=contain>` inside 96vw×95vh modal shell. Replaces hand-built `Diagram2D` React component. Deployed + pushed.
- 2026-04-22 (PHASE 1 — dashboard shell + encrypted secret store): `3b97b59`. Multi-session buildout starting. **Backend**: added `app_secrets` (AES-256-GCM at rest, reuses existing `ENCRYPTION_SECRET` master key) + `app_secret_audit` + `email_changes` tables in `internal/store/store.go` migrations. New `internal/store/secrets.go` with in-memory cache (sync.RWMutex) and `AllowedSecretKeys` map covering Stripe / Ping++ / PayPal / SMTP / CAPTCHA providers. Values are **write-only once saved** — admin API never returns the raw value. New endpoints: `GET /api/admin/settings`, `PUT/DELETE /api/admin/settings/{key}`, `GET /api/admin/settings/audit`, `GET /api/public/config` (publishable keys only), `POST /api/auth/email/change-request|change-confirm` (30-min 6-digit code). **Frontend**: `/dashboard` rebuilt as `layout.tsx` shell + 3 route-driven sub-pages (`/dashboard` profile, `/dashboard/payments` placeholder table, `/dashboard/server` placeholder credentials/status). Sidebar uses `usePathname()` for active state. New shared profile header (gold banner + overlapping avatar) matches mockups `login 3/4/6.png`. `/admin/settings` page with grouped provider cards, SET/NOT SET badges, masked last-four display, Update/Cancel/Clear actions, and recent-changes audit table. Admin overview gains a Settings quicklink. EN+ZH keys added under `dashboard.profile.*`, `dashboard.email.*`, `dashboard.paymentHistory.*`, `dashboard.serverInfo.*`, `admin.settings.*`. 30 routes build clean. Phases 2-5 remaining (configurator + Stripe/Ping++/PayPal + invoice PDF + 5-stage order stepper + server provisioning).
- 2026-04-22 (PHASE 2 — configurator + Stripe + catalog admin): `62f9d9d`. **Backend**: new tables `billing_cycles`, `support_tiers`, `server_tiers`, `promo_codes`, `orders`, `invoices` (seeded with 1-5 yr cycles, 5 support tiers, 3 server tiers at 1k/3k/5k chargers, 0 promos). New `internal/payments/stripe.go` — raw-HTTP Stripe client (no SDK, keeps binary at 10.65 MB): `CreateCheckoutSession` with inline `price_data`, `RetrieveCheckoutSession`, `VerifyStripeSignature` (HMAC-SHA256 v1, 5-min tolerance). New `cmd/server/billing.go` with catalog CRUD (admin) + public catalog read + promo apply + checkout (server-authoritative pricing) + webhook handler + user invoices/orders. Stripe keys loaded from encrypted `app_secrets` store — zero env edits required. **Frontend**: `/buy` configurator (payment-1 mockup, live total), `/buy/review` (payment-2 mockup, promo + billing form + payment method picker), `/buy/success` (payment-3 mockup, polls order until paid), `/admin/products` (inline CRUD for cycles/tiers/promos), `/dashboard/payments` now shows real invoices + pending orders. 34 routes build clean. Frontend 1135 files (7m 60s SFTP), backend 10.65 MB + systemd restart (17.2s). Smoke test: `GET /api/products/catalog` returns seeded 5 billing cycles. Phase 3 next (Ping++ + PayPal).
- 2026-04-22 (PHASE 3 — PayPal + Ping++): `882fbf2`. **PayPal (Orders v2)** in `internal/payments/paypal.go`: OAuth2 token cached ~9h, `CreateOrder` (intent=CAPTURE, `NO_SHIPPING`, `PAY_NOW`, `invoice_id` + `custom_id`), `CaptureOrder`, `VerifyWebhookSignature` against PayPal's verify-webhook-signature API. Flow: redirect to approve URL → `?paypal_order=` param on return → frontend POSTs `/api/billing/paypal/capture` to finalize, webhook `/api/webhooks/paypal` is authoritative backup on `PAYMENT.CAPTURE.COMPLETED`. **Ping++ (Alipay / WeChat Pay)** in `internal/payments/pingxx.go`: 2022-07-01 signature scheme — canonical `uri\nmethod\nts\nquery\nbody\n` → RSA-SHA256 signed with merchant PKCS1/PKCS8 PEM private key. `CreateCharge` with channel `alipay_pc_direct` extracts the redirect URL from `credential.alipay_pc_direct.url`. Webhook `/api/webhooks/pingxx` verifies RSA-SHA256 with Ping++ published public key. Locale-based default provider in `/buy/review`: zh → pingxx, otherwise stripe. Success page handles PayPal capture ("Confirming payment with PayPal…" transient state). Admin copy fix: "Operations console for the GCSS platform" replaces "demo environment" language (EN + ZH). Also dropped "Demo website" framing from `CLAUDE.md`. Frontend 1135 files (5m 48s SFTP), backend 13.4s restart. Phase 4 (invoice PDF + 5-stage order stepper) and Phase 5 (server info credentials + provisioning tracker) remaining.
- 2026-04-22 (PHASE 4 — orders + invoices): `5439552` + `61f60ef`. 5-stage dual steppers (`OrderStages` = received/processing/provisioning/testing/ready, `ServerStages` = not_started/starting/deploying/configuring/ready) on new `/dashboard/orders?n=<orderNumber>` page (auto-polls every 10s). Printable invoice at `/invoices?n=<number>` using browser print-to-PDF (white-sheet layout, @media print hides app chrome). Admin `/admin/orders` with inline stage selectors + manual "Mark paid" for bank-transfer scenarios. Query params instead of dynamic segments because `output:'export'` static export rejects un-enumerated `[param]` segments (61f60ef is the fix after initial deploy failed with "missing generateStaticParams").
- 2026-04-22 (PHASE 4.1 — code-review cleanup): `f9490f7`. Addresses the `superpowers:code-reviewer` report's ship-blockers: **H1** Stripe webhook now fetches invoice via new `RetrieveInvoice` and persists `hosted_invoice_url` + the real invoice ID; **H3** Ping++ now converts USD-cents → CNY fen using new admin secret `PINGXX_USD_TO_CNY_RATE` (checkout returns 503 with clear message if unset — prevents 7× under-charging); **H2** renamed `PINGXX_WEBHOOK_SECRET` → `PINGXX_WEBHOOK_PUBLIC_KEY` with relabeled admin UI ("it's a public key, not a secret"); **H4** dropped dead `{id}` placeholder in PayPal ReturnURL; **H5** `MarkOrderPaidWith` short-circuits on already-paid orders (prevents UNIQUE-constraint errors on concurrent mark-paid); **C2** Stripe signature tolerance uses `abs(time.Since)` for future-dated rejection; **G2** app_secrets audit "set" vs "update" now determined by pre-upsert lookup (fixed off-by-one); **G4** catalog-load errors logged with 500 not silent fall-through; **G7** Ping++ `credential.alipay_pc_direct` handled as both string and object; new `GetOrderByNumberForUser` DB query replaces linear scan; new `MarkPaidInput` struct separates `provider_payment_id` from `provider_invoice_id`; admin orders dropdowns now show translated stage labels. Backend 14.0s, frontend 1212 files (7m 0s). Phase 5 (Server Info credentials + provisioning tracker) is the only remaining phase.
- 2026-04-22 (PHASE 5 — server info + provisioning): `55eacfe`. New `user_servers` table with per-user CPMS record (region, api_base_url, api_key_hash (sha256), api_key_last4, ocpp_endpoint, webhook_url, status {provisioning|active|degraded|suspended}, uptime_pct, connected/max chargers, last_backup_at, notes). **Plaintext API keys are NEVER stored** — only sha256 hash + last4. Format `gcss_<32B base64url>`. `EnsureUserServerForOrder` is idempotent. `MarkOrderPaidWith` auto-provisions a server row after commit with max_chargers from the order's server_tier. `RotateAPIKey` returns plaintext once. Endpoints: `GET /api/user/server`, `POST /api/user/server/rotate`, `GET /api/admin/servers`, `PUT /api/admin/servers/{id}` (status whitelist), `POST /api/admin/servers/{id}/regenerate-key`. **Frontend** `/dashboard/server` shows credentials card (copy + rotate) + status card (region/uptime/chargers/last backup) with one-time plaintext-key reveal banner after rotation. **Admin** `/admin/servers` expandable card per customer with all editable fields + regenerate-key. Admin overview gains Server Instances quicklink. EN+ZH i18n for `dashboard.serverInfo.*`, `admin.servers.*`. Backend 15.4s, frontend 1239 files (7m 20s). **All 5 dashboard/billing phases complete.**
- 2026-04-23: Pricing copy tweaks per user. `pricing.saas.f4` changed from "Stripe 支付（择一）" to "从现有支付系统中择一启用" / "Choose one from existing payment methods" (clarifies customer picks one method, not that Stripe is mandatory). `pricing.dedicated.priceNote` "年付 $1,500" → "$1,500 /年（年度折扣结算）" / "$1,500 /year (annual billing)". `pricing.selfhosted.title` "自建或专属部署" → "私有化部署安装" / "Private Deployment & Installation". `pricing.selfhosted.desc` replaced with PDF wording: "系统以安装包形式交付，可部署于用户自有服务器或租赁 GCSS 服务器。" / "Delivered as an installation package. Deploy on your own server or lease a GCSS server." `pricing.hosted.desc` EN tightened to match Chinese.
- 2026-04-23 (bank transfer + deposit flow): Big payment-methods feature. **Business rules** (matching price PDF + user request): (a) platform plans (appent/webplat/appplat) with total > $1,000 offer a "Pay $1,000 deposit online + balance via bank transfer" option; (b) any charge > $1,500 is forced to bank transfer server-side; (c) bank transfer is always available as a payment method alternative. **Backend**: new `internal/store/bank.go` with `BankAccount` CRUD + `BankSlip` (upload, list per order, list per status, approve/reject in a transaction that also flips order status to paid/deposit_paid). New `cmd/server/bank.go` with public `GET /api/bank-accounts`, admin CRUD `/api/admin/bank-accounts[/{id}]`, customer `POST /api/billing/bank-slip` + `GET /api/user/orders/{num}/slips`, admin `GET /api/admin/bank-slips?status=X` + `POST /api/admin/bank-slips/{id}/approve|reject`. Orders gained nullable `deposit_cents` + `balance_cents` columns (forward migration via ALTER TABLE) and new `NewOrderInput.Status` field so `awaiting_transfer` orders are created without going through a gateway. `handlePlanCheckout` extended: (1) `useDeposit` request flag gated to platform plans where total > $1k, reduces charge to DepositCents ($1000) and sets balance_cents = total - 1000; (2) bank_transfer provider creates the order with status='awaiting_transfer' and returns a URL pointing to `/buy/bank-transfer` instead of a gateway; (3) server-side gate rejects non-bank-transfer providers when chargeAmount > BankTransferThresholdCents ($1500) with explicit error code `bank_transfer_required`. Slip upload endpoint validates `slipUrl` must start with `/uploads/` to block arbitrary URLs. Approving a "balance" or "full" slip marks the order paid + advances order_stage to processing in one transaction; approving a "deposit" slip transitions pending → deposit_paid. **Frontend**: `/buy/review` — new `useDeposit` state + `bank_transfer` provider tile + deposit-option card shown only when eligible; `mustUseBankTransfer` auto-switches provider when the charge amount exceeds $1500, hides online gateways with an inline policy banner; summary sidebar gains a split-row block showing "Due now (online)" + "Balance (bank transfer)" when deposit is on, or "Bank transfer total" when the provider is bank_transfer. New `/buy/bank-transfer?order=N` page: centered layout with an Order summary card (ref number with copy btn, product, total, deposit paid, balance due, status pill), Bank accounts card iterating the public endpoint with a full `<dl>` per bank (bank name, account name, account number w/ copy, SWIFT, IBAN, address, notes) + a gold Reference callout telling the customer to include the order number in the wire memo, and an Upload-slip form (bank picker + note field + file input accepting image/PDF) that POSTs to `/api/uploads` then records a BankSlip. Shows a history list of previously submitted slips with status pill + admin notes + a "View slip" link. **Admin**: `/admin` home gained two new nav cards — Bank Accounts and Bank Transfer Slips. New `/admin/bank-accounts` page lists rows with edit/delete buttons + an Add modal with a 2-column form grid (label, currency, bank name, account name, account number, SWIFT, IBAN, sort order) + full-width fields for bank address + notes textarea + Active checkbox. New `/admin/bank-slips` page with filter tabs (Awaiting review / Approved / Rejected / All), each row shows order number, user email, amount, kind, bank, timestamp, product label, customer note, admin note, View slip link, and Approve/Reject buttons for pending rows — both actions open a modal with a notes textarea before committing. Status pills (`.bt-pill--review/approved/rejected`) shared with the buy side. **Translations**: 50+ new keys added in EN+ZH — full `bankTransfer.*` top-level block (40 keys), `admin.bankAccounts.*` (28 keys), `admin.bankSlips.*` (24 keys), `admin.nav.bankAccounts/bankSlips`, `buy.review.providerBank/Desc`, `buy.review.bankTransferRequired`, `buy.review.deposit*`, `buy.summary.dueNow/dueLater/bankTransferAmount`. **CSS** (~500 lines added to pages.css): `.buy-deposit-option/--active/body/head/title/chip/help`, `.buy-summary-split/-row/-amt`, `.bt-layout/-header/-title/-subtitle/-grid/-main/-card/-card-title/-card-desc/-meta/-row/-k/-v/-banks/-bank/-bank-head/-bank-name/-bank-currency/-bank-dl/-bank-v/-copy/-notice/-form/-field-help/-empty/-slips/-slip/-slip-head/-slip-kind/-slip-meta/-slip-note/-slip-admin/-slip-link/-pill`, `.admin-page-head/-back-link/-empty/-list/-row/-row-body/-row-head/-row-sub/-row-meta/-row-product/-row-note/-row-actions/-filter-row/-filter-tab/-modal/-modal-body/-modal-actions`, `.form-grid-2`, `.form-check`, `.form-banner--info`, `.btn-danger`. Backend deployed (16.5s + 15.0s — two deploys). Frontend deployed (1320 files, 10m 38s). Smoke test: `GET /api/bank-accounts` returns `{"accounts":[]}` — admin needs to add bank rows at `/admin/bank-accounts` to enable the flow.
- 2026-04-23 (Dedicated removal + hero cleanup + rich summary + contact form): **Homepage hero**: deleted the 4 floating SVG icons (`hero-floating-icon.float-3d.hero-float-1..4`) and the surrounding `.hero-visual` column. `.hero-split` went from `1fr 1fr` to `minmax(0, 780px)` centered so the remaining text block reads as a clean centered hero without a lonely empty right column. **Dedicated Server Rental plan removed**: backend `internal/store/plans.go` dropped the `dedicated` entry from `PlanCatalog()` + the `case "dedicated"` branches in `PriceFor()` (both subtotal + label paths), resequenced sortOrder (saas=1, customweb=2, appent=3, webplat=4, appplat=5). Frontend `/pricing`: removed from `hostedPlans` array (now 1 SaaS card, wrapped in `max-width: 420px; margin: 0 auto` for a centered solo card instead of leaving it stranded in a 2-col grid), dropped the Dedicated `<th>` column, chained a `.map()` over `tableSections` that filters index 1 out of every row's cells array (one-line surgical change vs editing 56 individual arrays), `colSpan={7}→6`. `/buy`: seedDefaults no longer references dedicated, `planHeadline` lost its dedicated case, obsolete `computePrice` dedicated branch cleaned during shared-pricing extraction (next item). **Rich itemised summary** on /buy and /buy/review: extracted `computePrice`, `buildLineItems`, `buildMetaRows`, `formatUSD`, `pickLabel` into new `src/lib/buy/pricing.ts` to avoid drift between pages. Summary now shows: (1) **Meta block** — Plan / Deployment / Billing / Term / Chargers (SaaS) / Hosting term (one-time w/ hosting) / Addons count as small-caps k/v rows; (2) **Line items** with label + gray-tertiary detail + right-aligned amount (e.g. `Software setup · One-time · $300`, `Monthly hosting · 12 × $120/mo · $1,440`, `Extra payment gateway · 2 × $1,000 · $2,000`); (3) Subtotal (shown when >1 line); (4) Discount (review page only); (5) Total. CSS added for `.buy-summary-meta/-row/-k/-v`, `.buy-summary-line--rich`, `.buy-summary-line-main/-label/-detail/-amt`, plus override of the global `.buy-summary-line span:first-child` ellipsis for rich rows. 14 new translation keys under `buy.*` and `buy.summary.*`. Legacy `summaryUnitLabel`/`cartHeadline` helpers deleted. **Contact form** (`/contact` card on the page): reordered so Country comes **before** Phone. Phone now has a `<select class="code code-select">` of ITU-T E.164 codes (27 options, sorted numerically) instead of the read-only `<div class="code">`. Added `COUNTRIES` constant (27 entries with name + code, e.g. `Hong Kong → +852`) that drives the country dropdown, and a `useEffect` on `formData.country` that pushes the matching code into `formData.phoneCode` — user can still override the code afterwards since the effect only fires when `country` changes. Custom dropdown chevron via inline SVG data URI to match existing field styling. Translation key `contact.form.phoneCodeLabel` added (EN+ZH). Backend deployed (10.97 MB, 16.5s). Frontend deployed (1239 files, 8m 29s). Smoke test: `GET /api/products/plans` returns 5 plans, dedicated absent.
- 2026-04-23 (/buy ↔ /pricing alignment): Rebuilt the configurator at `/buy` to match the 6 PDF plans on `/pricing`. **Backend** (`internal/store/plans.go` new): `Plan`, `Addon`, `PlanSelection` types with `PlanCatalog()` returning the 6 tiers (saas $84/yr/charger, dedicated $200/mo or $1,500/yr, customweb $300+$120/mo, appent $16,900, webplat $21,800, appplat $34,200) + 5 add-ons (mobile_lang/admin_lang $100, gateway/pos $1,000, customization $120/day). `PriceFor(sel)` is the server-authoritative pricing function — caps years at 1-5, validates SaaS charger count ≥ 1, and rejects unknown plan keys. New `/api/products/plans` public endpoint. `checkoutRequest` extended with `planKey/billingMode/years/chargers/withHosting/addons`; `handleCheckout` short-circuits to a new `handlePlanCheckout` when `planKey` is set, leaving the legacy custom-configurator path intact for backwards compat. `billing_cycle_id`/`support_tier_id`/`server_tier_id` are nullable, so plan-based orders skip them and store the plan label in `product_label`. **Frontend** (`buy/PageClient.tsx` rewritten): plan-card grid → per-plan options panel (SaaS shows charger-count input + years; Dedicated/CustomWeb show monthly|yearly toggle + years; AppEnt/WebPlat/AppPlat show iOS-style hosting toggle + conditional years) → add-ons list with +/− qty steppers → live-total summary sidebar. Reads `?plan=<key>` from URL so "Buy Now" on /pricing pre-selects the right plan. `useSearchParams` requires Suspense in static export, so wrapped `<PageClient/>` accordingly. Mirror of `PriceFor` lives client-side for the live total but server still validates. **Review page** (`buy/review/PageClient.tsx` rewritten): renders plan headline + add-on lines + promo + billing form + payment-method picker; sends new `planKey`-shaped payload via `apiCheckout()`. Stripe/PayPal/Pingxx providers unchanged — they receive `(productLabel, totalCents)` either way. **Pricing page**: "Buy Now" links now use `{pathname:'/buy', query:{plan:<key>}}` so each card jumps to the correct preselected plan. **Translations**: 17 new `buy.*` keys (choosePlan, familyHosted/Private, configure, addonsTitle/Subtitle, increment/decrement, chargersLabel, billingModeLabel, billingMonthly/Yearly, yearsLabel, hostingLabel/Help/YearsLabel, oneTime, hosting). **CSS** (~210 lines added in pages.css): `.buy-plan-grid` / `.buy-plan-card` / `.buy-options-stack` / `.buy-option-row` / `.buy-option-toggle` (pill segmented) / `.buy-toggle` (iOS-style switch) / `.buy-addons-list` / `.buy-addon-row` / `.buy-qty-btn` (30px square steppers) — all with focus-visible rings + hover states. Backend deployed (10.97 MB binary, 19.7s, service active). Frontend deployed (1239 files, 8m 2s). Smoke test: `GET /api/products/plans` returns all 6 plans + 5 add-ons.
- 2026-04-23 (comparison table expansion): Rebuilt the pricing comparison table to match all rows in the official price PDF (`GCSS软件平台报价单.pdf`). Was 21 rows in 3 categories (CPMS / Super Admin / Description); now 56 rows in 7 categories: 费用 (5 rows: hosting fee, charger count, license fee, billing cycle, annual discount), 基础服务 (11 rows: driver app, OCPP, gateway, mobile/admin lang, API, custom logo $12,800, app stores $12,800, custom domain, server, data ownership), CPMS / 后台 / CPO (13 rows incl. fault notification 1,000-emails-then-$6/1k, sub-account, fleet, RFID), 司机/用户管理 (4 rows: balance view/adjust, user export, wallet history), B2B 功能 (7 rows — only enabled for Web APP 平台版 + APP 平台版), 司机端 APP (9 rows incl. tap-to-charge for native APP only, login providers Facebook/邮箱/Google/Apple), 远程维护操作 (7 rows incl. OCPP log retention 90天/无限制). Rewrote tbody with structured `tableSections` data: each row carries a label string + array of cells where `true|false` renders ✓/✕ with aria-label and any string renders verbatim. Added 70+ new translation keys under `pricing.table.*` (replaced legacy keys). Added `aria.included`/`aria.notIncluded`. CSS polish: `min-width: 980px` + `table-layout: fixed` so cells don't squish (parent already has `overflow-x: auto`), 24% first-column / 12.66% rest, word-wrap for long Chinese strings, left-aligned category-row labels, subtle gold tint on row hover. Build green (1239 files), deployed via SFTP in 8m 12s.
- 2026-04-22 (UX polish day): 15 commits `282c34a..9cc3612`. **Bug fixes**: empty audit table blew up React (nil slice → JSON null, now `[]`; defensive `?? []` guards across admin APIs); nginx `/uploads/` was being swallowed by static-file regex — added `^~` prefix modifier + bumped `client_max_body_size 10m → 25m`. **ui-ux-pro-max passes**: dashboard shell touch targets + active-nav left-border + focus-visible rings + reduced-motion; dashboard profile restructured to stats strip + section cards with icon headers; softened stats-row typography (was Bloomberg-terminal aggressive); admin overview KPI grid switched from 7-col squish to auto-fit with 7-color left accents; admin nav cards relaid to left-aligned icon-top-label-below; admin sub-pages got gold hairline header accent, hover-tinted field rows, sticky table headers + row hover wash, SET badge now green; admin/* bottom padding 96→140px. **Avatar + cover uploads** (`788ffe9`): new `users.avatar_url` + `cover_url` columns (fwd migration), `AuthUser` JSON gains fields, new `PUT /api/auth/profile/images` endpoint validates URLs must be `/uploads/*` (no external trackers). Frontend shows glass "Change cover" pill + circular gold camera-FAB on avatar; 10MB client-side cap. **Fixed cover→avatar z-index overlap** (`.dash-profile-identity { z-index: 2 }`). **Modal image-wrap** got `#2b3340` slate bg matching the B2B/B2C PNG backgrounds. **accessibility-compliance:ui-visual-validator audit** (`95fb9c4`) — 3 Critical + 5 High fixed: C1 stepper ARIA semantics (aria-label + aria-current="step" + visually-hidden "Step N of M: X — complete"), C2 all 11 `/buy/review` billing inputs now have id+htmlFor pairs + autoComplete tokens (given-name, family-name, email, tel, organization, address-line1/2, address-level1/2, country-name, postal-code), C3 global skip-nav link (`#main-content`, gold pill on focus), H1 new `--primary-text: #8C5F00` token for AA contrast on light-mode cards (applied to planchip, subscription-chip, sidebar-btn.active, section-icon, buy-option-price, dash-link), H2 focus-visible rings on buy-option + buy-provider, H3 checkout validation focuses first invalid field + sets aria-invalid, H4 5 user-facing gateway error paths no longer leak raw Go error text (Stripe/PayPal/Pingxx), H5 admin settings fields wrapped in role="group" + aria-labelledby. **Pricing page rebuild** (`9cc3612`): 6 tiers matching official GCSS PDF across Hosted Mode (2 tiers: SaaS Hosted $84/yr/charger, Dedicated Server Rental $200/mo) + Private Deployment (4 tiers: Custom Web APP, APP Enterprise $16,900 [was wrongly $13,900], Web APP Platform $21,800 [was wrongly $18,800], APP Platform $34,200). Dropped "H5 Free" (not in PDF). New Add-ons section. Feature-comparison table now 6-column. All commits pushed to GitHub; last live deploy 1239 files, 7m 2s.
